---
const { classStyle, id, dataAos } = Astro.props;
---

<div
  data-aos={dataAos}
  id={id}
  class={` ${classStyle} my-4 backdrop-filter backdrop-blur-lg bg-neutral-50 dark:bg-slate-800/95 w-3/4 mx-auto p-8 rounded-2xl shadow-xl`}
>
  <slot />
</div>
